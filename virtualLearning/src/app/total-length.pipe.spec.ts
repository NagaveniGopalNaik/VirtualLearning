import { TotalLengthPipe } from './total-length.pipe';

describe('TotalLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
